# ================================
# Beltche MCP Server Configuration
# ================================

# OAuth Configuration (required)
# Get these from FusionAuth admin panel
OAUTH_CLIENT_ID=your-client-id-from-fusionauth
OAUTH_CLIENT_SECRET=your-client-secret-from-fusionauth
OAUTH_AUTHORIZE_URL=https://auth.beltche.com/oauth2/authorize
OAUTH_TOKEN_URL=https://auth.beltche.com/oauth2/token
OAUTH_REDIRECT_URI=http://localhost:3000/auth/callback
OAUTH_SCOPE=openid profile email

# Server Configuration
PORT=3000
NODE_ENV=development

# Beltche API
BELTCHE_API_BASE_URL=https://beltche.com/api/v1

# Redis (optional - uses in-memory store if not provided)
# REDIS_URL=redis://localhost:6379

# Rate Limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100
